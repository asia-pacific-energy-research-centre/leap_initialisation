# `99 AUTO BALANCE` mapping and end-to-end follow-through

**Status:** active execution prompt; created 2026-08-25.

**Purpose:** follow the synthetic `99 AUTO BALANCE` transfer fuel from its
canonical display-name authority through the `leap_mappings` refresh pipeline,
LEAP template verification, and a fresh all-template baseline-seed validation.

This prompt is deliberately scoped to the synthetic balancing counterpart. It
must not turn `99 AUTO BALANCE` into a real ESTO/9th product relationship or
silence unrelated mapping and template findings.

## Current evidence

- `leap_initialisation/codebase/transfers_workflow.py` defines:
  - ESTO-side label: `99 AUTO BALANCE`;
  - LEAP fuel name: `AUTO BALANCE`;
  - product code: `99`.
- The one-sided transfer policy adds a synthetic counterpart only when a
  transfer is inflow-only or outflow-only. The amount is at least 1 PJ and may
  be larger to satisfy the configured efficiency ceiling. Measured ESTO values
  must remain unchanged.
- `docs/handover_transfers_auto_balance_20260820.md` records that the active
  economy templates contain the 72 AUTO BALANCE rows per template, with
  placeholder BranchID `100`. These are placeholders, not real LEAP IDs.
- The completed seed run `run_583459` still emitted warnings such as
  `Failed to resolve label name for 99 AUTO BALANCE` and
  `Failed to map label 99 AUTO BALANCE`.
- The canonical mappings reference is
  `C:\Users\Work\github\leap_mappings\docs\mappings_system.md`.

## Operating rules

1. Read the current `AGENTS.md` files in both repositories and run
   `git status --short` in both before editing.
2. Do not edit `leap_mappings/config/outlook_mappings_master.xlsx` generated
   pair sheets directly. The maintained mapping contract is
   `config/outlook_mappings_single_axis.xlsx`; generated compatibility outputs
   must be regenerated by the canonical pipeline.
3. First identify where the current pipeline gets the preserved
   `leap_display_names` authority. If the source is not obvious, inspect the
   active refresh code and manifest before editing. Do not create a second
   local crosswalk in `leap_initialisation`.
4. Do not add `99 AUTO BALANCE` to `leap_combined_esto`,
   `leap_combined_ninth`, `ninth_pairs_to_esto_pairs`, or any ordinary
   sector/fuel axis sheet unless separate semantic evidence proves it is a real
   comparison category. The expected change is a display-name/fuel-catalog
   authority change, not a fabricated energy-balance relationship.
5. Never copy BranchIDs between economies. BranchIDs are area-specific.
   Placeholder `100` must be replaced only by IDs obtained from the matching
   real LEAP area export.
6. Keep the synthetic fuel out of real-energy balance totals and conservation
   checks. Validate the real transfer products separately from the synthetic
   counterpart rows.
7. Do not launch a long baseline-seed workflow until the mapping refresh and
   targeted checks pass. Use a unique explicit run label if a full run is
   approved.

## Phase 1 — inspect the current authority and establish a baseline

In `C:\Users\Work\github\leap_mappings`:

- Confirm the current mapping source, generated master, live/fallback
  resolution, and mapping commit hash.
- Inspect the exact columns and existing rows in `leap_display_names`.
  Determine whether `99 AUTO BALANCE` or `AUTO BALANCE` already exists under a
  different code type or label.
- Search the entire mapping repository for both strings and record every
  operational consumer and every warning-producing path.
- Confirm the current `leap_initialisation` templates and the 72-row AUTO
  BALANCE coverage per active economy. Record whether each row has BranchID
  `100`, `-1`, or a real LEAP ID, and confirm that the branch paths and variable
  rows match the intended transfer roots.
- Capture a small pre-change baseline:
  - current mapping workbook hashes;
  - current display-name lookup result;
  - current fuel-catalog preflight result;
  - current count of AUTO BALANCE mapping warnings in the latest seed log;
  - current real-product transfer totals and synthetic-row totals for the
    affected economies.

Stop and report if the display-name source is generated-only and there is no
supported editable authority. Do not patch the generated master as a shortcut.

## Phase 2 — make the canonical display-name change

Add the reviewed display-name authority entry using the existing schema and
conventions. The intended semantic record is:

```text
code_type: esto_product
code: 99 AUTO BALANCE
leap_display_name: AUTO BALANCE
USED_IN_LEAP_INITIALISATION: 1
```

Use the repository's actual column names, casing, note fields, and source
workbook location after inspection. Preserve existing rows and formatting.
Add an auditable note explaining that this is a synthetic transfer-balancing
counterpart and is not a real ESTO/9th energy product.

Before accepting the change, check:

- no duplicate `(code_type, code)` authority rows;
- no duplicate display-name authority that would resolve the same synthetic
  fuel ambiguously;
- no new source-to-target mapping relationship was created;
- no ordinary ESTO/9th balance total now includes the synthetic fuel by virtue
  of the display-name change.

## Phase 3 — run and verify the canonical mappings pipeline

From `C:\Users\Work\github\leap_mappings`:

1. Run the pipeline help and confirm the current stage names and refresh
   options. The current documented entry point is
   `codebase/run_mapping_pipeline.py`.
2. Because a maintained mapping input changed, run the explicit generation
   path before Stages 1–3. Do not run Stages 1–3 against the old compatibility
   master.
3. Start with the repository's safe smoke scope and then run the intentional
   all-economy scope with `--leap-economies all`, while keeping deep recursive
   validation disabled unless disk and runtime capacity have been checked and
   the user explicitly approves it.
4. Check `results/logs/mapping_pipeline.log` and the generated refresh
   manifest. Confirm that:
   - the display-name row is present in the generated compatibility master;
   - `AUTO BALANCE` resolves through the unified name lookup;
   - duplicate, schema, source-fingerprint, promotion, and generated-master
     integrity checks pass;
   - no new ordinary `leap_combined_esto` or `leap_combined_ninth` relationship
     was introduced;
   - any remaining mapping QA finding is classified as non-blocking or
     explicitly reported as a blocker.

Record the generated workbook hash, manifest path, pipeline log path, and
commit hash. Do not delete QA evidence.

## Phase 4 — verify LEAP template and fuel-catalog readiness

In `leap_initialisation`:

- Run the configured mapping-workbook resolver and confirm the live canonical
  mapping master is selected, or document why the vendored fallback is being
  used.
- Run the fuel-catalog preflight against the available LEAP/template inputs.
  Confirm that the synthetic fuel resolves as `AUTO BALANCE`, not merely as
  the raw label `99 AUTO BALANCE`.
- For every active template economy, verify all intended AUTO BALANCE branch
  paths exist under:
  - `Transformation\Transfers unallocated\...`;
  - `Transformation\Refinery and blending transfers\...`; and
  - `Transformation\Upstream liquids transfers\...`.
- Confirm the 72-row coverage per template and verify that no BranchID from
  another economy has been copied in.
- If the active template still contains placeholder BranchID `100`, treat the
  template as structurally prepared but not yet LEAP-area import-ready. Obtain
  a fresh full-model export from that real LEAP area and refresh the template
  before claiming ID readiness.
- Do not invent IDs, assume that placeholder `100` is a valid LEAP branch ID,
  or use one economy's IDs for another.

## Phase 5 — regenerate and validate baseline seeds

Only after Phases 1–4 pass:

- Use the active baseline-seed preset and all 11 current template economies:
  `01_AUS, 02_BD, 05_PRC, 10_MAS, 11_MEX, 12_NZ, 13_PNG, 15_PHL, 19_THA,
  20_USA, 21_VN`.
- Use the full configured horizon 2022–2060 and all configured scenarios.
- Use a new explicit run label; never reuse `run_583459` or an automatic label
  that can collide with an existing run.
- Before launch, verify there are no active economy locks and record the exact
  interpreter, script, label, scope, horizon, and log paths.
- After completion, inspect:
  - baseline-seed validation findings and issue groups;
  - final-artifact manifest/audit;
  - missing branch and invalid-ID findings;
  - fuel-catalog and display-name warnings;
  - transfer workbook rows containing `AUTO BALANCE`;
  - real-product transfer rows before and after the change.

The expected result is that the display-name warnings disappear or reduce to
only genuinely unresolved LEAP-area/template issues, while the synthetic rows
remain isolated and real measured transfer values are unchanged.

## Phase 6 — balance and conservation checks

Run targeted checks that:

- exclude `AUTO BALANCE` from real-energy balance totals;
- preserve signed real transfer products and all non-synthetic rows;
- show synthetic counterpart rows only for one-sided transfer cases;
- confirm the process-efficiency ceiling is still respected;
- confirm no duplicate logical keys or cross-economy ID borrowing were created;
- compare the generated seed against the pre-change seed on all real fuels;
- report any differences in `AUTO BALANCE` rows separately from real-energy
  differences.

Do not treat a clean display-name lookup as proof that the real LEAP branches
exist. Mapping readiness and LEAP-area ID readiness are separate gates.

## Required final report

Report:

1. the edited canonical source and commit hash;
2. the generated mapping master, refresh manifest, and pipeline log;
3. before/after display-name and fuel-catalog lookup results;
4. template coverage and real-ID status by economy;
5. baseline-seed run label, horizon, scenarios, and output directory;
6. remaining warnings/errors grouped into mapping, template, ID, and balance
   categories;
7. real-product conservation results and isolated AUTO BALANCE results; and
8. any required follow-up action before LEAP import.

If any hard gate fails, stop before import and state the exact file, economy,
branch path, or mapping relationship that caused the stop.
