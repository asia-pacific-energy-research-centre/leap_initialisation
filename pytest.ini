[pytest]
testpaths = tests
norecursedirs = tmp .git .claude outputs
addopts = -m "not integration"
markers =
    integration: expensive real-data, process, package, or full-render verification; run with -m integration
